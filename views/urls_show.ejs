<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />

  <%- include('partials/_stylesheet') %>

    <title>URL - TinyApp</title>
</head>

<body>

  <%- include('partials/_header') %>

    <main style="margin: 1em;">
      <% if(!user){ %>
        <h3>Please log in or register</h3>
        <% } else{ %>
          <div class="card text-center">
            <div class="card-header"></div>
            <div class="card-body">
              <h5 class="card-title">TinyURL for: <%= longURL %>
              </h5>
              <p class="card-text">
                Short URL ID: <a href="/u/<%= urlID %>">
                  <%= urlID %>
                </a>
              </p>
            </div>
            <div class="card-footer text-muted">
              <h2 class="card-title">Edit</h2>
              <form class="form-inline d-flex align-items-center justify-content-center" action="/urls/<%= urlID %>"
                method="POST">
                <div class="form-group mb-2">
                  <label for="newURL">New URL:</label>
                  <input class="form-control" type="text" name="newURL" placeholder="http://"
                    style="width: 300px; margin: 1em " />
                  <button type="submit" class="btn btn-primary">Submit</button>
                </div>
              </form>
            </div>
          </div>

          <% } %>
    </main>

    <%- include('partials/_scripts') %>

</body>

</html>